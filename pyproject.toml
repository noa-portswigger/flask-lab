[project]
name = "flask-lab"
version = "0.1.0"
description = "A Flask application for experimentation"
requires-python = ">=3.11"
dependencies = [
    "flask>=3.0.0",
    "gunicorn>=23.0.0",
]

[tool.uv]
package = true
